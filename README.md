
  # CubeTimer

  This is a code bundle for CubeTimer. The original project is available at https://www.figma.com/design/uZL1tXnZq2U5D4KWu5dtKm/CubeTimer.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  